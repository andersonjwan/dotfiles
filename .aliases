#!/usr/bin/env bash

# Navigational shortcuts
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

alias clear='echo "Reminder: Use Ctrl+L to clear the screen!"'
alias ls='ls --group-directories-first --color=auto'
